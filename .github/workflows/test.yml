name: Run tests
on:
  push
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:

      - name: get up on this
        uses: actions/checkout@v2
        with:
          ref: develop

      - name: set up nox and pythons
        uses: excitedleigh/setup-nox@v2.0.0

      - name: hit it
        run: nox
